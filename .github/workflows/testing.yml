# This is a basic workflow that is manually triggered

name: Test

on:
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
    - name: Send greeting
      run: echo "Hello ${{ GITHUB_ENV }}"
